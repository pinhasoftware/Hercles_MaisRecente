import { StubPage } from "@/components/StubPage";

export default function ClientWorkout() {
  return <StubPage title="Treino" subtitle="Sessão ativa, exercícios e registo de séries." back="/app" />;
}